# ── Secrets and local config ──
.env
.envrc

# ── Downloaded models, cache, temp ──
models/
input/
tmp/
finbert/
rag_cache/

# ── Generated experiment outputs ──
output/
plots/
results/
external_review_report/

# ── Documentation (paper source, intermediate builds) ──
docs/

# ── Byte-compiled ──
__pycache__/
*.py[codz]
*$py.class

# ── Package builds ──
build/
dist/
*.egg-info/
*.egg
.installed.cfg

# ── Test and lint caches ──
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# ── Virtual environments ──
.venv
venv/
env/

# ── OS / editor cruft ──
.DS_Store
*.swp
*.swo

# ── Raw data artifacts (download from source) — ignore large originals ──
data/raw/**/*.csv
data/raw/**/*.xlsx
data/raw/**/*.zip
data/finance/*.csv
data/health/*.csv
data/health/*.xlsx
data/health/*.zip

# But track the exact processed test/corpus files used by the experiment
!data/finance/finance_test_500.csv
!data/finance/finance_corpus.csv
!data/health/covid_test_500.csv
!data/health/covid_corpus.csv

# ── LaTeX build artifacts ──
*.synctex.gz
*.out
*.aux
*.log
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.ist

# ── Project instructions ──
CLAUDE.md

# ── Jupyter / Marimo ──
.ipynb_checkpoints
__marimo__/
